class Conversor(object):
  def __init__(self):
    graph = Graph()
  def __str__(self):
  
def to_json(string):
  arr = string.split('')
  insert_into_graph(arr)

def insert_into_graph(arr):
  for index in range(len(arr)):
    graph.add_node(index)
